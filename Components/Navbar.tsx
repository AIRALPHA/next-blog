import { Navbar as MantineNavbar, Center, Tooltip, UnstyledButton, createStyles, Group } from '@mantine/core';

export default function Navbar() {
  return (
    <MantineNavbar color="primary">
      Nav
    </MantineNavbar>
  )
}
